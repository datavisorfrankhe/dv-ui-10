zE(function () {
  zE.hide()
})

window.zESettings = {
  webWidget: {
    contactForm: {
      fields: [
        { id: 'description',
          prefill: { '*': 'Suggestions for improvement' }
        }
      ],
      title: {
        '*': 'NEW DASHBOARD FEEDBACK'
      }
    }
  }
}
